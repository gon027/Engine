/*
#include "../../engine/include/Engine.hpp"

// シューティングゲームのサンプル
void gnMain() {
	using namespace engine;

	App::init("A", 640, 480);
	Scene::setBackGroundColor({ 0.0f, 1.0f, 1.0f, 1.0f });

	while (App::update()) {
		App::begin();

		App::end();
		Sleep(1);
	}
}
*/